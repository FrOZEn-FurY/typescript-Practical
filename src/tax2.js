/**
 * This is the second version of the tax calculation, we can provide information as you see like this.
 *  @param {number} income // This is how we annotate parameters.
 *  @returns {number} // This is how we annotate return type.
 */

export default function calculateTax2(income) {
    return income * 9 / 100;
}